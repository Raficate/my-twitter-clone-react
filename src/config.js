const config = {
    baseURL_API: process.env.REACT_APP_APIURI,
    clientID: process.env.REACT_APP_CLIENTID //Todas las varibles de entorno definidas por nosottos en React tienen que empezar por REACT_APP_
}

export default config;